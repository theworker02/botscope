# Example BotScope plugin (TEMPLATE)

This directory demonstrates a minimal classifier plugin shape.

Run validation:

```bash
python -c "from botscope.plugins.sdk import validate_plugin_dir; print(validate_plugin_dir('examples/plugin_example'))"
```

Status: TEMPLATE / EXPERIMENTAL — not auto-loaded unless registered via entry points.
