# Tutorial 1 — Doctor and demo

```bash
pip install -e ".[dev]"
botscope doctor
botscope demo --output demo_analysis.bscope --json
```

Confirm the DEMO notice appears. Open the session with `botscope open demo_analysis.bscope`.
